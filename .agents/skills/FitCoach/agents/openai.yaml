interface:
  display_name: "FitCoach"
  short_description: "Repo-specific patterns and workflows for FitCoach"
  default_prompt: "Use the FitCoach repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true