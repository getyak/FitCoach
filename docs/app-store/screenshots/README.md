# App Store 截图

`6.9-inch/` 中的三张截图均为 iPhone 17 Pro Max 模拟器直接输出的 1320 × 2868 PNG，没有设备边框、营销文字或后期修改：

1. `01-today.png`：今天页与“沿用上次并开始”入口。
2. `02-training-rest.png`：渐进式当前组、上次表现与休息计时。
3. `03-completion.png`：完成总结、剩余课时与趋势入口。

截图使用 `DemoDataSeeder` 的确定性虚构学员数据，不包含真实用户信息。训练与完成截图来自 `testCoachCanContinuePreviousWorkoutAndConsumeOneCredit` 的真实九组 UI E2E 流程，而不是静态 mock。
