FitCoach 无需账号登录，也没有付费墙。

建议审核路径：
1. 首次启动后添加一位学员，并填写可选课时。
2. 在学员页创建第一节训练，添加动作和训练组。
3. 开始训练，完成任意一组即可看到 App 内休息计时。
4. 完成课程后可检查训练总结和课时变化。

本地通知仅在用户于“我的”中主动开启并授权后使用。Live Activity 用于显示休息倒计时，不包含学员姓名或健康数据，也不使用远程推送。

备份由用户主动导出为 JSON 文件；应用不运营云端账户或业务服务器。
